/**
 * @description Handles the change event for form inputs and updates the form state.
 * @param e - The event object from the form input change event.
 * @param setFormData - The state setter function to update the form data.
 */

// import { ChangeEvent } from "react";

// const handleChange = (
//     e: ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>,
//     setFormData: React.Dispatch<React.SetStateAction<Record<string, string>>>
// ): void => {
//     const { name, value } = e.target;
//     setFormData((prevData) => ({
//         ...prevData,
//         [name]: value,
//     }));
// };

// export default handleChange;
